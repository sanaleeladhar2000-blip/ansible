- name: demo on loops
  hosts: local
  connection: local
  tasks:
  - name: print names one by one
    ansible.builtin.debug:
      msg: "Hello {{ item }}"
    loop: 
      - Ramesh
      - Suresh
      - Mahesh